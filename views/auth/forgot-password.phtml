<div class="centered-container" style="max-width: 400px; padding: var(--spacing-xxl) var(--spacing-xl);">
    <h1 style="text-align: center; margin-bottom: var(--spacing-sm);"><?= $t->trans('FORGOT_PASSWORD') ?? 'Forgot Password' ?></h1>
    <p style="text-align: center; margin-bottom: var(--spacing-md); font-size: 0.9rem; opacity: 0.7;">
        <?= $t->trans('FORGOT_PASSWORD_DESC') ?? 'Enter your email address and we will send you a link to reset your password.' ?>
    </p>

    <?php if (isset($error)): ?>
        <div class="alert alert-error" style="margin-bottom: var(--spacing-md); text-align: center;">
            <?= \htmlspecialchars($error) ?>
        </div>
    <?php endif; ?>

    <?php if (isset($success)): ?>
        <div class="alert alert-success" style="margin-bottom: var(--spacing-md); text-align: center;">
            <?= \htmlspecialchars($success) ?>
        </div>
    <?php endif; ?>

    <form action="/forgot-password" method="POST" class="minimal-form">
        <input type="hidden" name="csrf_token" value="<?= $csrf_token ?>">
        <input type="email" name="email" placeholder="<?= $t->trans('EMAIL') ?>" class="input-field" required>
        <button type="submit"><?= $t->trans('SEND_RESET_LINK') ?? 'Send Reset Link' ?></button>
    </form>
    
    <p style="margin-top: var(--spacing-md); text-align: center; font-size: 0.8rem; opacity: 0.6;">
        <a href="/login" class="text-link" style="color: inherit; text-decoration: underline;"><?= $t->trans('BACK_TO_LOGIN') ?? 'Back to login' ?></a>
    </p>
</div>