<?php
    /** @var \Afterchange\Template\Utils\Translator $t */
    /** @var string $csrf_token */
    /** @var string $token */
    /** @var string $error */
?>
<div class="centered-container" style="max-width: 400px; padding: var(--spacing-xxl) var(--spacing-xl);">
    <h1 style="text-align: center; margin-bottom: var(--spacing-sm);"><?= $t->trans('RESET_PASSWORD') ?? 'Reset Password' ?></h1>
    <p style="text-align: center; margin-bottom: var(--spacing-md); font-size: 0.9rem; opacity: 0.7;">
        <?= $t->trans('RESET_PASSWORD_DESC') ?? 'Please enter your new password below.' ?>
    </p>

    <?php if (isset($error)): ?>
        <div class="alert alert-error" style="margin-bottom: var(--spacing-md); text-align: center;">
            <?= \htmlspecialchars($error) ?>
        </div>
    <?php endif; ?>

    <form action="/reset-password" method="POST" class="minimal-form">
        <input type="hidden" name="csrf_token" value="<?= $csrf_token ?>">
        <input type="hidden" name="token" value="<?= \htmlspecialchars($token ?? '') ?>">
        <input type="password" name="password" placeholder="<?= $t->trans('NEW_PASSWORD') ?? 'New Password' ?>" class="input-field" minlength="8" required>
        <button type="submit"><?= $t->trans('SUBMIT_RESET') ?? 'Reset Password' ?></button>
    </form>
</div>